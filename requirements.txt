# Code CCTV DevLoop — pinned dependencies for reproducible builds
# Python 3.10+ required
#
# Install:  pip install -r requirements.txt
#
# Core DevLoop runtime
agentscope==2.0.5
# Optional model SDK used by the local AgentScope experiment path.
# This is not an AgentTeams runtime dependency.
dashscope==1.26.5
# A DeepSeek credential is configured through DEEPSEEK_API_KEY when that
# experimental path is explicitly selected.

# Credential & config
PyYAML==6.0.2
pydantic==2.13.4
pydantic-core==2.46.4

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0

# The management UI is served from web/ and has no native desktop dependency.
# Real AgentTeams deployment dependencies are intentionally absent until the
# control plane, credentials and workflow integration are configured.
